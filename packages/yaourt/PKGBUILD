# This file is part of BlackArch Linux ( http://blackarch.org ).
# See COPYING for license details.

pkgname='yaourt'
pkgver='1.9'
pkgrel=1
pkgdesc='A pacman wrapper with extended features and AUR support.'
arch=('any')
url='http://www.archlinux.fr/yaourt-en/'
license=('GPL')
depends=('diffutils' 'pacman' 'package-query' 'gettext')
optdepends=('aurvote: vote for favorite packages from AUR'
            'customizepkg: automatically modify PKGBUILD during install/upgrade'
            'rsync: retrieve PKGBUILD from official repositories')
makedepends=('git')
backup=('etc/yaourtrc')
source=("http://mir.archlinux.fr/releases/yaourt/yaourt-${pkgver}.tar.gz")
sha512sums=('303aca4a07ff00a2ef9fd1116b1bca2124d3614f9ac8f2e4cf73e0b27246859a4d8997220813d9339adce9665a8d736aad227a6cec73b515d472f0518bbc5659')

build() {
  cd "$srcdir/yaourt-$pkgver"

  make PREFIX=/usr sysconfdir=/etc localstatedir=/var
}

package() {
  cd "$srcdir/yaourt-$pkgver"

  make PREFIX=/usr sysconfdir=/etc localstatedir=/var DESTDIR="$pkgdir" install
}

