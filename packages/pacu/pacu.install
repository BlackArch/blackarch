post_install() {
  set -e
  cd /usr/share/pacu
  python -m venv venv
  source venv/bin/activate &&
    pip install --isolated --root="/usr/share/pacu" --prefix='venv' .
}

post_upgrade() {
  post_install "$@"
}

post_remove() {
  rm -r /usr/share/pacu
}
