# This file is part of BlackArch Linux ( https://www.blackarch.org/ ).
# See COPYING for license details.

pkgname=ruby-snmp
_gemname=snmp
pkgver=1.3.3
pkgrel=1
pkgdesc='A Ruby implementation of SNMP (the Simple Network Management Protocol).'
arch=('any')
license=('MIT')
makedepends=('ruby')
url='https://rubygems.org/gems/snmp'
source=("https://rubygems.org/gems/$_pkgname-$pkgver.gem")
sha512sums=('4528bafc71d9948c09d129c78faa25202b9248d7d6d3c76f4474ba8c916f74a47adab55b053ae3aed6946a9d5853bd96eb797ab8a7bd1b172ee9839f93abf105')
noextract=("$_gemname-$pkgver.gem")

package() {
  _gemdir=$(ruby -e 'puts Gem.default_dir')

  if [[ $CARCH == arm* ]] ; then
    gem install --no-user-install --ignore-dependencies --no-rdoc --no-ri \
      -i "$pkgdir$_gemdir" "$_gemname-$pkgver.gem"
  else
    gem install --no-user-install --ignore-dependencies -i "$pkgdir$_gemdir" \
      -n "$pkgdir/usr/bin" "$_gemname-$pkgver.gem"
  fi
}

