post_install() {
  set -e
  cd /usr/share/firebaseEnum
  python -m venv venv
  source venv/bin/activate &&
    pip install --isolated --root="/usr/share/firebaseEnum" --prefix='venv' \
      -r requirements.txt
}

post_upgrade() {
  post_install "$@"
}

post_remove() {
  rm -r /usr/share/firebaseEnum
}
