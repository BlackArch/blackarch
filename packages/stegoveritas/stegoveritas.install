post_install() {
  set -e
  cd /usr/share/stegoveritas
  python -m venv venv
  source venv/bin/activate &&
    pip install --isolated --root="/usr/share/stegoveritas" --prefix='venv' \
    -r requirements.txt
}

post_upgrade() {
  post_install "$@"
}
