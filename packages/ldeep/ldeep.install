post_install() {
  set -e
  cd /usr/share/ldeep
  python -m venv venv
  source venv/bin/activate &&
    pip install --isolated --root="/usr/share/ldeep" --prefix='venv' .
}

post_upgrade() {
  post_install "$@"
}

post_remove() {
  # /usr/share/ldeep/venv
  rm -r /usr/share/ldeep
}
