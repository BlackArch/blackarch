pkgname=beef
pkgver=0.4.4.5
pkgrel=1
pkgdesc="the browser exploitation framework is a penetration testing tool that focuses on the web browser"
arch=("${CARCH}")
groups=('blackarch-exploitation' 'blackarch-all')
url=('http://www.beefproject.com')
license=('BSD')
source=("https://github.com/beefproject/beef/archive/${pkgname}-${pkgver}.zip")
depends=('ruby' 'sqlite')
md5sums=('cc97a4cad3501006bce0df7735681c79')
options=('!strip')

package() {
  cd "${srcdir}"/${pkgname}-${pkgname}-${pkgver}
}
