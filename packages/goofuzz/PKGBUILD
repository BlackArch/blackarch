# This file is part of BlackArch Linux ( https://www.blackarch.org/ ).
# See COPYING for license details.

pkgname=goofuzz
_name=GooFuzz
pkgver=1.2.2.r2.g4063720
pkgrel=1
pkgdesc='GooFuzz is a script written in Bash Scripting that uses advanced Google search techniques to obtain sensitive information in files or directories without making requests to the web server.'
arch=('any')
groups=('blackarch' 'blackarch-fuzzer' 'blackarch-scanner')
url='https://github.com/m3n0sd0n4ld/GooFuzz'
license=('GPL3')
depends=()
makedepends=('git')
source=("git+https://github.com/m3n0sd0n4ld/$_name")
sha512sums=('SKIP')

pkgver() {
  cd "$_name"

  git describe --long --tags | sed 's/\([^-]*-g\)/r\1/;s/-/./g'
}

package() {
  cd "$_name"

  install -dm 755 "$pkgdir/usr/share/$pkgname"

  install -Dm 755 $_name "$pkgdir/usr/bin/$pkgname"
  cp -a wordlists "$pkgdir/usr/share/$pkgname"
}

