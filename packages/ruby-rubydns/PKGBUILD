# Maintainer: lsfxz (https://github.com/lsfxz/quarry)
# Many thanks to anatol for quarry and gem2arch!
# This file is part of BlackArch Linux ( http://blackarch.org ).
# See COPYING for license details.
_gemname=rubydns
pkgname=ruby-$_gemname
pkgver=1.0.3
pkgrel=2
pkgdesc=An\ easy\ to\ use\ DNS\ server\ and\ resolver\ for\ Ruby.
arch=(any)
url=http://www.codeotaku.com/projects/rubydns
license=(MIT)
depends=(ruby ruby-celluloid-0.16 ruby-celluloid-io-0.16 ruby-timers-4.0)
makedepends=()
groups=()
replaces=()
conflicts=()
provides=()
optdepends=()
options=(!emptydirs)
source=(https://rubygems.org/downloads/$_gemname-$pkgver.gem
    
)
noextract=($_gemname-$pkgver.gem)
sha1sums=('9185f807598cc52df3cd12a2a424ba81ee4dd882'
    
)

prepare() {

  true
}

package() {
  local _gemdir="/usr/lib/ruby/gems/2.3.0"
  gem install --ignore-dependencies --no-document --no-user-install -i "$pkgdir/$_gemdir" -n "$pkgdir"/usr/bin $_gemname-$pkgver.gem 
  rm "$pkgdir/$_gemdir/cache/$_gemname-$pkgver.gem"
  local _extdir="$pkgdir//usr/lib/ruby/gems/2.3.0/extensions/$CARCH/2.3.0/$_gemname-$pkgver"
  if [ -d "$_extdir" ]; then
    rm -rf "$_extdir"/*
    touch "$_extdir/gem.build_complete"
  fi

  rm -rf "$pkgdir/$_gemdir/gems/$_gemname-$pkgver"/{.gitignore,.simplecov,.travis.yml,.yardopts,Gemfile,README.md,Rakefile,examples,rubydns.gemspec,spec}
}
