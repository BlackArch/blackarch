# Generated by gem2arch (https://github.com/anatol/gem2arch)
# Maintainer: Jörg Thalheim <joerg@higgsboson.tk>

_gemname=ruby-beautify
pkgname=ruby-$_gemname
pkgver=187.30f2d27
_gemver=0.97.4
pkgrel=2
pkgdesc='prettifier/beautifier for Ruby code [from http://www.arachnoid.com/ruby/]'
arch=(any)
url='http://github.com/rogerdpack/erniebrodin/ruby-beautify'
license=()
depends=(ruby)
options=(!emptydirs)
makedepends=(git)
source=('git+https://github.com/lsfxz/ruby-beautify.git')
sha1sums=('SKIP')

pkgver() {
	cd "$srcdir/$_gemname"

  echo $(git rev-list --count HEAD).$(git rev-parse --short HEAD)
}


build() {
  cd "$srcdir/$_gemname"

  gem build $_gemname.gemspec
}

package() {
  cd "$srcdir/$_gemname"

  local _gemdir="$(ruby -e'puts Gem.default_dir')"
  gem install --ignore-dependencies --no-user-install -i "$pkgdir/$_gemdir" -n "$pkgdir/usr/bin" $_gemname-$_gemver.gem
  rm "$pkgdir/$_gemdir/cache/$_gemname-$_gemver.gem"
  install -D -m644 "$pkgdir/$_gemdir/gems/$_gemname-$_gemver/LICENSE" "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  cd "$pkgdir/usr/bin"
  ln -s ruby-beautify rbeautify
}
