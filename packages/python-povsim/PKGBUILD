# This file is part of BlackArch Linux ( https://www.blackarch.org/ ).
# See COPYING for license details.

pkgname=python-povsim
_pkgname=povsim
pkgver=1.7.0
pkgrel=1
pkgdesc='POV simulation for CGC'
arch=('any')
url='https://github.com/mechaphish/povsim'
license=('BSD-2-Clause')
depends=('python' 'python-shellphish-qemu')
makedepends=('python-build' 'python-pip' 'python-wheel')
source=("https://files.pythonhosted.org/packages/source/${_pkgname::1}/$_pkgname/$_pkgname-$pkgver.tar.gz")
sha512sums=('e1f57e8af768157aa984367bdfec6a4a809bf56254e26cd8e10a2383afcb3f336e794738466a6e7c1c0ed9115b98099a4156ae4a182e40e8d09cc4c606b69645')

build() {
  cd "$_pkgname-$pkgver"

  python -m build --wheel --outdir="$startdir/dist"
}

package() {
  cd "$_pkgname-$pkgver"

  pip install \
    --verbose \
    --disable-pip-version-check \
    --no-warn-script-location \
    --ignore-installed \
    --no-compile \
    --no-deps \
    --root="$pkgdir" \
    --prefix=/usr \
    --no-index \
    --find-links="file://$startdir/dist" \
    $_pkgname
}

