post_install() {
  set -e
  cd /usr/share/bbot
  python -m venv venv
  source venv/bin/activate &&
    pip install --isolated --root='/usr/share/bbot' --prefix='venv' .
}

post_upgrade() {
  post_install "$@"
}

post_remove() {
  # /usr/share/bbot/venv
  rm -r /usr/share/bbot
}

