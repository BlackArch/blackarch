--- a/common/include/istypes.h	2025-09-30 20:34:16.393267236 +0200
+++ b/common/include/istypes.h	2025-09-30 20:32:32.659806668 +0200
@@ -28,7 +28,22 @@
 #define __ISFTYPES_H__
 
 /** boolean type */
-typedef unsigned char bool;
+#ifdef __cplusplus
+/* C++ has built-in bool */
+#else
+#  include <stdbool.h>
+  /* On very old C compilers without stdbool.h, fall back to our own definition */
+#  if !defined(__bool_true_false_are_defined)
+     typedef unsigned char bool;
+#    ifndef true
+#      define true 1
+#    endif
+#    ifndef false
+#      define false 0
+#    endif
+#  endif
+#endif
+
 #define TRUE     (0==0)
 #define FALSE    (!TRUE)

--- a/system/dema/include/dema.h	2025-10-01 00:08:53.818383275 +0200
+++ b/system/dema/include/dema.h	2025-10-01 00:11:13.167397442 +0200
@@ -82,7 +82,22 @@
 
 
 /** boolean type */
-typedef unsigned char bool;
+#ifdef __cplusplus
+/* C++ has built-in bool */
+#else
+#  include <stdbool.h>
+  /* On very old C compilers without stdbool.h, fall back to our own definition */
+#  if !defined(__bool_true_false_are_defined)
+     typedef unsigned char bool;
+#    ifndef true
+#      define true 1
+#    endif
+#    ifndef false
+#      define false 0
+#    endif
+#  endif
+#endif
+
 #define TRUE     (0==0)
 #define FALSE    (!TRUE)

