pkgname=vega
pkgver=
pkgrel=1
pkgdesc="An open source platform to test the security of web applications"
arch=('x86_64' 'i686')
url=""
license=('')
# TODO: Add to secondary groups.
groups=(blackarch)
depends=('java-runtime')
source=("http://www.subgraph.com/downloads/VegaBuild-linux.gtk.$CARCH.zip")
md5sums=()

pkgver() {
  date +%Y%m%d
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
  # I give up.
}
