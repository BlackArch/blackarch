# Maintainer: lsfxz (https://github.com/lsfxz/quarry)
# Many thanks to anatol for quarry and gem2arch!
# This file is part of BlackArch Linux ( http://blackarch.org ).
# See COPYING for license details.
pkgname='ruby-celluloid-essentials'
_gemname='celluloid-essentials'
pkgver='0.20.5'
pkgrel=2
pkgdesc="Internally used tools, and superstructural dependencies of Celluloid"
arch=('any')
url='https://github.com/celluloid/celluloid-essentials'
license=("MIT")
depends=('ruby' 'ruby-timers')
options=('!emptydirs')
source=("https://rubygems.org/downloads/$_gemname-$pkgver.gem" )
noextract=("$_gemname-$pkgver.gem")
sha1sums=('c314d6f673354eb41e3e27b793d41c300a920078' )


package() {
  local _gemdir="$(ruby -rubygems -e 'puts Gem.default_dir')"
  gem install --ignore-dependencies --no-document --no-user-install -i "$pkgdir/$_gemdir" -n "$pkgdir"/usr/bin $_gemname-$pkgver.gem 
  rm "$pkgdir/$_gemdir/cache/$_gemname-$pkgver.gem"
  install -D -m644 "$pkgdir/$_gemdir/gems/$_gemname-$pkgver/LICENSE.txt" "$pkgdir/usr/share/licenses/$pkgname/LICENSE.txt"

  rm -rf "$pkgdir/$_gemdir/gems/$_gemname-$pkgver"/{.env-ci,.env-dev,.gitignore,.gitmodules,.rspec,.rubocop.yml,.travis.yml,CHANGES.md,Gemfile,LICENSE.txt,README.md,Rakefile,celluloid-essentials.gemspec,tasks}
}
