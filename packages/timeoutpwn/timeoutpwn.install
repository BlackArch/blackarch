post_install() {
  echo 'The exploit files can be found in /usr/share/timeoutpwn.'
  echo 'By default, the files are installed with permissions disabled for anyone but the root user.'
  echo 'Undo this at your own risk. Make sure your system is not vulnerable first.'
}
