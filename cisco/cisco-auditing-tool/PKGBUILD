# Contributor: Francesco Piccinno <stack.box@gmail.com>

pkgname=cisco-auditing-tool
pkgver=1
pkgrel=1
pkgdesc="Perl script which scans cisco routers for common vulnerabilities."
url="http://www.scrypt.net/~g0ne/"
groups=('archpwn' 'archpwn-scanners' 'archpwn-cisco')
arch=(any)
license=('GPL')
depends=('perl')
source=(http://packetstormsecurity.org/cisco/CiscoAuditingTool-v$pkgver.tar.gz)
md5sums=('8d3a3e32c04648d29469a9f37e65caf3')

build() {
  cd "$srcdir/CiscoAuditingTool"

  install -d $pkgdir/usr/bin
  install -d $pkgdir/usr/share/$pkgname

  cp -r * $pkgdir/usr/share/$pkgname/ || return 1
  chmod 0755 $pkgdir/usr/share/$pkgname/CAT || return 1
  cat > $pkgdir/usr/bin/$pkgname << EOF
#!/bin/sh
cd /usr/share/$pkgname/
perl CAT \$*
EOF
  chmod 0755 $pkgdir/usr/bin/$pkgname
}

# vim:set ts=2 sw=2 et:
