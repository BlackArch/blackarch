# Contributor: Francesco Piccinno <stack.box@gmail.com>

pkgname=perl-net-packet
pkgver=3.26
pkgrel=1
pkgdesc="a framework to easily send and receive frames from layer 2 to layer 7"
arch=('i686' 'x86_64')
url="http://search.cpan.org/~muir/Net-Packet"
license=('PerlArtistic')
depends=('perl' 'perl-bit-vector' 'perl-net-ipv4addr' 'perl-net-ipv6addr' 'perl-socket6' 'perl-net-libdnet' 'perl-net-pcap' 'perl-net-write')
source=(http://search.cpan.org/CPAN/authors/id/G/GO/GOMOR/Net-Packet-$pkgver.tar.gz)
md5sums=('19f7fb5b5b5eb509e44a2a6e189fc2c9')

build() {
  cd  $startdir/src/Net-Packet-$pkgver
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1 
  make || return 1
  make pure_install doc_install DESTDIR=$startdir/pkg || return 1
  find $startdir/pkg -name '.packlist' -delete
  find $startdir/pkg -name '*.pod' -delete
}
