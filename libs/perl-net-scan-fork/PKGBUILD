# This PKGBUILD was generated by cpan4pacman via CPANPLUS::Dist::Pacman
# Contributor: Francesco Piccinno <stack.box@gmail.com>

pkgname=perl-net-scan-fork
_cpanname=Net-Scan-Fork
pkgver=0.01
pkgrel=1
pkgdesc="Net::Scan::Fork"
arch=('any')
url="http://search.cpan.org/~MCANTONI/${_cpanname}"
license=('GPL' 'PerlArtistic')
depends=('perl-sys-cpuload>=0.02')
options=('!emptydirs')
source=(http://www.cpan.org/authors/id/M/MC/MCANTONI/${_cpanname}-${pkgver}.tar.gz) 
md5sums=('bdfb0f72ba002e9c98845b8a7ade8588')

build() {
  cd $srcdir/${_cpanname}
  sed -i '/^auto_install/d' Makefile.PL
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1 
  make || return 1
  make install DESTDIR=$pkgdir || return 1
  find $pkgdir -name '.packlist' -delete
  find $pkgdir -name '*.pod' -delete
}
