# Contributor: Francesco Piccinno <stack.box@gmail.com>

pkgname=perl-net-write
pkgver=1.04
pkgrel=1
pkgdesc="a portable interface to open and send raw data to network"
arch=('i686' 'x86_64')
url="http://search.cpan.org/~gomor/Net-Write"
license=('PerlArtistic')
depends=('perl' 'perl-net-pcap' 'perl-class-gomor' 'perl-socket6')
source=(http://search.cpan.org/CPAN/authors/id/G/GO/GOMOR/Net-Write-$pkgver.tar.gz)
md5sums=('f01fe8c303307c0cbf1ec16dc94d2b74')

build() {
  cd  $startdir/src/Net-Write-$pkgver
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1 
  make || return 1
  make pure_install doc_install DESTDIR=$startdir/pkg || return 1
  find $startdir/pkg -name '.packlist' -delete
  find $startdir/pkg -name '*.pod' -delete
}
