# This PKGBUILD was generated by cpan4pacman via CPANPLUS::Dist::Pacman
# Contributor: Francesco Piccinno <stack.box@gmail.com>
# Renamed by Wallnuss perl-net-ssh fits better than perl-...-perl

pkgname=perl-net-ssh
_cpanname=Net-SSH-Perl
pkgver=1.34
pkgrel=1
pkgdesc="Perl client Interface to SSH"
arch=('any')
url="http://search.cpan.org/~TURNSTEP/${_cpanname}"
license=('GPL' 'PerlArtistic')
depends=('perl-crypt-idea' 'perl-convert-pem>=0.05' 'perl-string-crc32>=1.2' 'perl-crypt-dh>=0.01' 'perl-digest-hmac' 'perl-crypt-des' 'perl-math-pari>=2.001804' 'perl-crypt-dsa>=0.11' 'perl-math-gmp>=1.04' 'perl-digest-sha1>=2.10')
options=('!emptydirs')
source=(http://www.cpan.org/authors/id/T/TU/TURNSTEP/${_cpanname}-${pkgver}.tar.gz) 
md5sums=('5b6d60e459707058e856c45c30e96e53')

build() {
  cd $srcdir/${_cpanname}-${pkgver}
  sed -i '/^auto_install/d' Makefile.PL
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1 
  make || return 1
  make install DESTDIR=$pkgdir || return 1
  find $pkgdir -name '.packlist' -delete
  find $pkgdir -name '*.pod' -delete
}
