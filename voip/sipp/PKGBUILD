# Contributor: Francesco Piccinno <stack.box@gmail.com>

pkgname=sipp
pkgver=3.1
pkgrel=1
pkgdesc="Sipp is a performance testing tool for the SIP protocol"
url="http://"
groups=('archpwn' 'archpwn-voip')
license=('GPL')
depends=('libpcap')
arch=(i686 x86_64)
source=(http://downloads.sourceforge.net/$pkgname/${pkgname}.${pkgver}.src.tar.gz)

build() {
  cd "$srcdir/$pkgname.svn"
  sed -ie "/<stdlib.h>/a#include <limits.h>" sipp.hpp

  make || return 1

  install -d $pkgdir/usr/bin
  install -d $pkgdir/usr/share/$pkgname

  install -Dm755 $pkgname $pkgdir/usr/bin/
  cp -r pcap $pkgdir/usr/share/$pkgname/
}

# vim:set ts=2 sw=2 et:
md5sums=('452a6f88f2d314dbb2f44e318a60982b')
