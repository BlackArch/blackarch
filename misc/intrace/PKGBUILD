# Contributor: Francesco Piccinno <stack.box@gmail.com>

pkgname=intrace
pkgver=1.4.3
pkgrel=1
pkgdesc="utility to run traceroute within an established TCP connection, thereby bypassing some stateful packet filters."
url="http://code.google.com/p/intrace/"
updateurl="http://code.google.com/p/intrace/downloads/list=>intrace-"
license=('GPL')
depends=('tcpdump')
makedepends=('make' 'gcc')
groups=('archpwn' 'archpwn-misc')
arch=(i686 x86_64)
provides=(intrace)
conflicts=(intrace)
source=(http://intrace.googlecode.com/files/$pkgname-$pkgver.tgz)
md5sums=('7cc6e6b2678c45549c5de8c98968bf18')

build() {
  cd "$srcdir/$pkgname-$pkgver"
  make || return 1
  install -Dm755 intrace $pkgdir/usr/bin/intrace || return 1
  install -Dm644 README $pkgdir/usr/share/doc/intrace/README || return 1
}

# vim:set ts=2 sw=2 et:
