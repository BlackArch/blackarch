# Maintainer: Ari Mizrahi <codemunchies@gmail.com>

pkgname=magictree
pkgver=1.2
pkgrel=1
pkgdesc="a penetration tester productivity tool designed to allow easy and straightforward data consolidation, querying, external command execution and report 
generation"
arch=('i686' 'x86_64')
url="http://www.gremwell.com"
license=('EULA')
source=(
	"http://www.blackarch.org/pub/packages/$pkgname-$pkgver.tar.gz"
	"http://www.blackarch.org/pub/desktops/blackarch-$pkgname.desktop"
)
depends=('jre7-openjdk' 'screen')
md5sums=('
	1f88da8cd57d570d0a0fe2ed459922a7
	0e20d5e972db8e2ccd66d2dff31f690a
')

package() {
  cd "$srcdir"
  install -Dm644 blackarch-${pkgname}.desktop ${pkgdir}/usr/share/applications/blackarch-${pkgname}.desktop

  cd "$srcdir/$pkgname-$pkgver"
  install -dm755 ${pkgdir}/blackarch/threat_modeling/magictree
  cp --preserve=mode -r * ${pkgdir}/blackarch/threat_modeling/magictree
}

