# Contributor: Francesco Piccinno <stack.box@gmail.com>

pkgname=fierce
pkgver=1.0.3
pkgrel=1
pkgdesc="Fierce is a reconnaissance tool that quickly scans domains using several tactics."
url="http://ha.ckers.org/fierce/"
arch=(any)
groups=('archpwn' 'archpwn-scanners')
license=('GPL')
depends=('perl' 'perl-net-dns')
source=(http://ha.ckers.org/fierce/$pkgname.pl http://ha.ckers.org/fierce/hosts.txt)
md5sums=('6e0cebe59b4e6189739cfee72d5a8c32'
         'e1f690cf33bce46715f17fd5d049082f')

build() {
  cd "$srcdir"

  install -d $pkgdir/usr/bin
  install -d $pkgdir/usr/share/$pkgname

  install -Dm755 $pkgname.pl $pkgdir/usr/bin/
  install -Dm644 hosts.txt $pkgdir/usr/share/$pkgname/
}

# vim:set ts=2 sw=2 et:
