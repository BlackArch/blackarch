#!/bin/bash

cd "$(dirname "$0")/.."

site=blackarch.org
dir=pub/blackarch
real_local=real-repo
real_local_bak=real-repo.bak

wget -r -np "$site/$dir"

mv "$real_local" "$real_local_bak"
mv "$site/$dir" "$real_local"
