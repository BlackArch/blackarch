#!/bin/bash
# List packages in the real repo that no longer exist in the Git repo.

jobs=10
base=$(dirname "$0")

printf '%s\n' "$base"/../real-repo/*/*.pkg.tar.xz |
  parallel --no-notice -j$jobs -n1 "$base/list-removed-helper"
