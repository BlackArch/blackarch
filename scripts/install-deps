#!/bin/bash
# Installs all package dependencies.
# Must be run from the repo root.

find . -name PKGBUILD | while read pack ; do
	source "$pack"
	deps=(${makedepends[@]} ${depends[@]})
	yaourt --asdeps --needed --noconfirm -S "${deps[@]}"
done
