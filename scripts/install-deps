#!/bin/bash

pkgbuild=$1

. "$pkgbuild"

for dep in "${depends[@]}" ; do
	# Strip the version specifiers.
	dep=$(sed 's/\(>\|<\|=\).*//' <<< $dep)

	pacman -S --needed --noconfirm --asdeps "$dep"
done
