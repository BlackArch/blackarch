#!/bin/bash
# Installs all package dependencies from the AUR and official repos.

asdeps=true
for pack in "$(dirname "$0")"/packages/* ; do
	(
	source "$pack/PKGBUILD"
	deps=(${makedepends[@]} ${depends[@]})
	yaourt --noconfirm -S ${deps[@]}
	$asdeps && sudo pacman -D --asdep ${deps[@]}
	)
done
