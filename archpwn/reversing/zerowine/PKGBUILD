# Contributor: Francesco Piccinno <stack.box@gmail.com>

pkgname=zerowine
pkgver=0.0.2
pkgrel=1
pkgdesc="Zero Wine Malware Analysis Tool - research project to dynamically analyze the behavior of malware"
url="http://zerowine.sf.net"
groups=('archpwn' 'archpwn-reversing')
license=('GPL')
depends=('python')
arch=(i686 x86_64)
source=(http://downloads.sourceforge.net/$pkgname/${pkgname}_vm-${pkgver}-src.tar.gz)
md5sums=('06f945e11b9c1f469d783b0e7cef95c3')

build() {
  cd "$srcdir"
  install -d $pkgdir/usr/share/zerowine/ || return 1
  cp -r $srcdir/${pkgname}_vm/src/* $pkgdir/usr/share/zerowine/ || return 1
  chmod 0755 $pkgdir/usr/share/zerowine/*.py
}

# vim:set ts=2 sw=2 et:
