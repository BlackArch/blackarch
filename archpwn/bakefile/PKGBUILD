# TODO: make package function

pkgname=bakefile
pkgver=0.2.8
pkgrel=1
pkgdesc="A cross-platform, cross-compiler native makefiles generator"
arch=('i686' 'x86_64')
url="http://bakefile.sourceforge.net/"
license=('MIT')
depends=('python')
options=('!libtool')
source=(http://downloads.sourceforge.net/bakefile/bakefile-$pkgver.tar.gz)
md5sums=('874da5c9035514366b7723bc7657c71c')

build() {
  cd "$srcdir/bakefile-$pkgver"
  ./configure --prefix=/usr
  make
  make DESTDIR="$pkgdir" install
  install -Dm644 COPYING "$pkgdir/usr/share/licenses/bakefile/COPYING"
}
