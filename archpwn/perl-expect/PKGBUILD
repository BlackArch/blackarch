# TODO: make package function
# This PKGBUILD was generated by cpan4pacman via CPANPLUS::Dist::Pacman

pkgname=perl-expect
_cpanname=Expect
pkgver=1.21
pkgrel=1
pkgdesc="Perl version of Don Libes' Tcl-Expect"
arch=('any')
url="http://search.cpan.org/~RGIERSIG/${_cpanname}"
license=('GPL' 'PerlArtistic')
depends=('perl-io-tty>=1.03')
options=('!emptydirs')
source=(http://www.cpan.org/authors/id/R/RG/RGIERSIG/${_cpanname}-${pkgver}.tar.gz)
md5sums=('a151b0dc4d1a35c73941c65b7c26da5b')

build() {
  cd $srcdir/${_cpanname}-${pkgver}
  sed -i '/^auto_install/d' Makefile.PL
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor
  make
  make install DESTDIR=$pkgdir
  find $pkgdir -name '.packlist' -delete
  find $pkgdir -name '*.pod' -delete
}
