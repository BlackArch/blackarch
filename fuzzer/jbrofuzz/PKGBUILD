# Contributor: Francesco Piccinno <stack.box@gmail.com>

pkgname=jbrofuzz
pkgver=1.7
pkgrel=1
pkgdesc="Web application protocol fuzzer that emerged from the needs of penetration testing."
url="http://sourceforge.net/projects/jbrofuzz/"
updateurl="http://sourceforge.net/projects/jbrofuzz/files/=>jbrofuzz-jar-(\\\d+)\.zip"
groups=('archpwn' 'archpwn-fuzzer')
license=('GPL')
depends=('java-runtime')
arch=('i686' 'x86_64')
source=(http://downloads.sourceforge.net/jbrofuzz/$pkgname-jar-17.zip)
md5sums=('b7b3a8dfe628b949686f1378fbb7d5f7')

build() {
  cd "$srcdir"
  install -d $pkgdir/usr/bin || return 1
  install -Dm644 JBroFuzz.jar $pkgdir/usr/share/$pkgname/JBroFuzz.jar || return 1

  cat > $pkgdir/usr/bin/jbrofuzz << EOF
#!/bin/sh
java -jar /usr/share/$pkgname/JBroFuzz.jar
EOF
  chmod 0755 $pkgdir/usr/bin/jbrofuzz || return 1
}

# vim:set ts=2 sw=2 et:
