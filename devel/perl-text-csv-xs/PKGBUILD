# This PKGBUILD was generated by cpan4pacman via CPANPLUS::Dist::Pacman
# Contributor: FranÃ§ois Charette <firmicus Î±Ï„ gmx Î´Î¿Ï„ net>

pkgname=perl-text-csv-xs
pkgver=0.60
pkgrel=1
pkgdesc="Fast 8bit clean version of Text::CSV"
arch=('i686' 'x86_64')
url="http://search.cpan.org/~HMBRAND/Text-CSV_XS"
license=('GPL' 'PerlArtistic')
depends=('perl>=5.10.0')
options=('!emptydirs')
source=(http://www.cpan.org/authors/id/H/HM/HMBRAND/Text-CSV_XS-$pkgver.tgz) 
md5sums=('a9f0934a58bba602a5d447792d9ded04')

build() {
  cd  $srcdir/Text-CSV_XS-$pkgver
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1 
  make || return 1
  make install DESTDIR=$pkgdir || return 1
  find $pkgdir -name '.packlist' -delete
  find $pkgdir -name '*.pod' -delete
}
