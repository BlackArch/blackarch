# Contributor: Francesco Piccinno <stack.box@gmail.com>

pkgname=perl-class-gomor
pkgver=1.01
pkgrel=1
pkgdesc="another class and object builder"
arch=('i686' 'x86_64')
url="http://search.cpan.org/~gomor/Class-Gomor-1.01/"
license=('PerlArtistic')
depends=('perl')
source=(http://search.cpan.org/CPAN/authors/id/G/GO/GOMOR/Class-Gomor-$pkgver.tar.gz)
md5sums=('53f611cd26439fa35ce1e84d700d777a')

build() {
  cd  $startdir/src/Class-Gomor-$pkgver
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1 
  make || return 1
  make pure_install doc_install DESTDIR=$startdir/pkg || return 1
  find $startdir/pkg -name '.packlist' -delete
  find $startdir/pkg -name '*.pod' -delete
}
