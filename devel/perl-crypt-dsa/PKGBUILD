# This PKGBUILD was generated by cpan4pacman via CPANPLUS::Dist::Pacman
# Contributor: Francesco Piccinno <stack.box@gmail.com>

pkgname=perl-crypt-dsa
_cpanname=Crypt-DSA
pkgver=1.16
pkgrel=1
pkgdesc="DSA signatures and key generation"
arch=('any')
url="http://search.cpan.org/~ADAMK/${_cpanname}"
license=('GPL' 'PerlArtistic')
depends=('perl-file-which>=0.05' 'perl-digest-sha1' 'perl-data-buffer>=0.01' 'perl>=5.10.0')
options=('!emptydirs')
source=(http://www.cpan.org/authors/id/A/AD/ADAMK/${_cpanname}-${pkgver}.tar.gz) 
md5sums=('a099204d57043767fbe31d639eb43b03')

build() {
  cd $srcdir/${_cpanname}-${pkgver}
  sed -i '/^auto_install/d' Makefile.PL
  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor || return 1 
  make || return 1
  make install DESTDIR=$pkgdir || return 1
  find $pkgdir -name '.packlist' -delete
  find $pkgdir -name '*.pod' -delete
}
